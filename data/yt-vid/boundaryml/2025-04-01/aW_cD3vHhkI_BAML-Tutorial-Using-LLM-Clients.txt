All right. So, you must be familiar with
BAML functions by now, and you've likely
noticed the client parameter within
those functions and wondered what's the
deal with that. Well, today we're going
to break it down. The client parameter
tells BAML which backend to use for
inference. You can pass it a string like
this,
provider/model name. Simple, right? So,
you could use open AI models,
enthropics, or a bunch of others. And
trust me, BAML supports a ton of
providers. Here's the list. Now, here's
the fun part. With time, you realize
that you don't always need big,
expensive models for every task. PAML's
optimizations allow you to make do with
smaller ones just fine. So, now you may
decide to switch to smaller and hence
cheaper models. Now, we don't want to go
ahead and update every single prompt
manually, right? Wouldn't it be great if
we had some sort of an LLM router which
allows us to easily control what model
gets used for what prompt? A router
which we can configure through our code.
Meet named clients. With named clients,
you can create reusable model
configurations. Something like this. If
you initialize your project with the
BAML CLI, you'll find a sample in the
autogenerated clients.baml file. Now,
here's the cool part. You can create as
many of these named clients as you want.
So I can have a small boy client for
lightweight task and a big boy client
for something more heavy duty. Simply
head over to your prompt and replace
this string with your client's name.
Wow, this is so cool. Now named clients
open up some new possibilities. Allow me
to share some tips. First, easy model
swaps. As we've already seen, you can
swap out the underlying models, even the
providers, without having to change your
prompts. Next, you can configure model
specific options such as temperature and
maximum tokens. This allows you to
create specialized name clients for
different scenarios. For example, a
creative light client with a high
temperature setting or a boring but
right client with focus on
predictability. Yeah, naming variables
is hard. Just keep in mind that various
providers support different options.
Check out your provider's documentation
to get a list of options they support.
You can find a link to that and more in
BML's docs. Here's another pro tip.
Yeah, I'm feeling generous today. If
your favorite provider isn't on BML's
list, don't panic. Many providers offer
an OpenAI compatible API. PAML's OpenAI
generic client can connect to any of
them. Just set the right base URL. Also,
do set the API key even if it's not
needed. It's an OpenAI quirk. For
example, here's how you can do it for
Grock. We've got more of these on our
docs. Now, the third tip. It is the
third tip, isn't it? We're going to call
this the third one. You can pass values
from environment variables as well. Just
prefix your environment variable name
with an env. You can use this to pass
sensitive values like base URLs and API
keys. Moving on to the harsh realities
of life. APIs fail. It happens. Maybe
OpenAI is having a bad day and doesn't
process your requests. Maybe Enthropic
decides to rate limit you. Maybe your
favorite openweight model is down.
Things can go wrong. Don't worry, we've
got your back. Remember when I showed
you the list of providers that BAML
supports? I'm sure something caught your
eye. You noticed it,
right? Right. So, the fallback provider
is like a super duper
client. I did not name that. It can try
different models in sequence. This
allows it to fall back from one name
client to another in a predetermined
order in case there are any network
issues or the API gives you an invalid
status code. To use the fallback
provider, just replace the client in
your prompt with this one. Yep, it's
just another named client. If you don't
want to hop around between providers and
just want BAML to retry after a delay,
you can use the retry policy. The retry
policy isn't really a provider. It's a
policy. I mean, it's in the name. You
can attach a retry policy with a
constant delay on an exponential back
off to any existing client. It's that
simple. Now, before I let you go,
there's just one more use case that I
absolutely need to talk about. Let's say
you're running a SAS company, cuz who
isn't? And you have different pricing
tiers for your users. What if you want
to use different models based on your
customers subscription? For example, use
a lightweight model for free tier users
and a more expensive model for premium
users. Hey, we all do that. I'm not
going to judge. What we need is the
ability to dynamically configure the
properties of a named client. Things
like the provider, the model being used,
actually everything through our code.
This is where the client registries
comes in. The idea is simple. We want to
swap out the config of a named client
object at runtime. You can do this in
three steps. First, create the client
registry object for each request. To be
fair, you could make this object global,
but in that case, you might want to
consider just sticking with named
clients instead, but again, you know
your use case better. Moving on, you
then add your LLM client object to this
registry. Here you need to pass in all
the parameters you want to set. Your
name registry is not merged but rather
replaced with the settings you pass.
Something to remember and it goes
without saying that you can pass in all
the options that you could have when
using named clients. There's nothing new
here. It is important to use the same
name as your name client. If you use a
different name, the BAML function will
default to the name client defined in
your BAML code. This is actually good as
it allows you to selectively replace
named clients depending on the context.
For example, you might want to switch to
a model with a huge context window if
you find yourself stuck in a longunning
conversation. You can always choose to
make any LLM client in this registry
primary. That would force the BAML
function to use this client no matter
what. Now the last step is to pass the
newly created client registry while
calling the BAML function. Don't forget
to do this. Skipping this step will make
your BML function fall back to the name
client all over again. And that's a
wrap. Name clients, fallback, retry
policies, and client registries. Bammer
gives you all the flexibility you could
possibly need. Got questions? Hit us up
on Discord. And don't forget to check
out the next video on dynamic types for
structure responses. I'll see you there.