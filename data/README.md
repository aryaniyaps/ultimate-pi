# Data corpus

- **`books/`** — PDFs (tracked with Git LFS). Category subfolders (`psychology/`, `software-engineering/`, etc.).
- **`youtube-transcripts/`** — YouTube transcript text (`.txt`), sidecar `.meta.txt`, and per-channel `_index.tsv`. Indexed with `scripts/index_youtube_urls.py` (defaults to this directory).
